/* See targ-cpu.h in this same directory: gas/configure.tgt's i386-*-pe
 * stanza answers fmt=coff, em=pe, so this is what a real ./configure
 * would have written here too.
 */
#include "obj-coff.h"
