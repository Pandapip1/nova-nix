/* Installed into the build tree as sys/file.h -- see build.kaem. */
/* Empty, as Mes's own is: bash includes <sys/file.h> for flock and the
   L_SET seek names, uses neither, and only needs the header to exist. */
#ifndef _SYS_FILE_H
#define _SYS_FILE_H
#endif
