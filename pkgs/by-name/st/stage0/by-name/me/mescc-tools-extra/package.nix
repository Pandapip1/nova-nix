{
  mescc-tools-extra-bin,
  sha256sum,
  match,
  mkdir,
  untar,
  ungz,
  unbz2,
  unxz,
  catm-mescc-tools,
  cp,
  chmod,
  rm,
  replace,
  wrap,
}:
{
  bin = mescc-tools-extra-bin;
  sha256sum = sha256sum;
  match = match;
  mkdir = mkdir;
  untar = untar;
  ungz = ungz;
  unbz2 = unbz2;
  unxz = unxz;
  catm = catm-mescc-tools;
  cp = cp;
  chmod = chmod;
  rm = rm;
  replace = replace;
  wrap = wrap;
}
