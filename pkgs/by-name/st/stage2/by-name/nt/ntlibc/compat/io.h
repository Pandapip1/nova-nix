#ifndef _NTLIBC_COMPAT_IO_H
#define _NTLIBC_COMPAT_IO_H

/* Native-Windows compatibility name used by gnulib.  ntlibc exposes the
   corresponding descriptor and file APIs through their POSIX headers. */
#include <unistd.h>
#include <fcntl.h>
#include <sys/stat.h>

#endif
