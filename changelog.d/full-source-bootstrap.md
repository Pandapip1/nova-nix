- **Linux and Windows can bootstrap complete native toolchains from source.** The unified `pkgs/by-name` package set now traces from platform stage0 sources through shared Mes/TinyCC layers, platform-selected libc, GNU userland, binutils, and modern GCC; the Windows stdenv builds ordinary packages with the resulting PE32 toolchain rather than the MSYS2 compiler seed.
